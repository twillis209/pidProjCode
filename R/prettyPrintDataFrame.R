#' @title Prints data frame in an org mode-compatible format
#'
#' @description Used to print (small) data frames suitable for copying into an org mode file
#'
#'
prettyPrintDataFrame <- function(dataFrame) {
  outputString <- ''

  if(!is.null(col.names(dataFrame))) {

  }

  if(!is.null(row.names(dataFrame))) {
    outputString <- 
  }
